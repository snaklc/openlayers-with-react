import React from 'react'

interface Props {
    style: object,
    label: string,
    onClick: Function
}

export const Button = (props:Props) => {
    return (
        <div>
            
        </div>
    )
}
